<section class="k4lt-section k4lt-scrollY">
  <select name="system.category">{{selectOptions appearanceOptions selected=system.category}}</select>
  {{formInput systemFields.description value=system.description enriched=system.description toggled=true engine="prosemirror"}}
</section>
